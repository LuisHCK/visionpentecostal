<% if admin_signed_in? %>
  <div class="row">
  <ul class="thumbnails files">
<% unless @pictures.empty? %>
  <% @pictures.each do |pic| %>
    <li class="span3" id="picture_<%= pic.id %>">
    <div class="thumbnail">
      <%= image_tag pic.image.url %>
      <div class="center-xs">
        <%=
          link_to "Borrar Foto", pic,
            :data => { :confirm => "¿Estas seguro?" },
            :method => :delete, class:"btn "
          %>

      </div>
    </div>
    </li>
  <% end %>
<% end %>
</ul>
</div>
<% end -%>